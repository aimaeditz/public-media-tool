{
  "name": "public-media-tool-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "install": "npm --prefix app-source install",
    "dev": "npm --prefix app-source run dev",
    "build": "npm --prefix app-source run build:root",
    "build:root": "npm --prefix app-source run build:root",
    "lint": "npm --prefix app-source run lint",
    "postinstall": "npm --prefix app-source install"
  },
  "dependencies": {
    "jspdf": "^3.0.1",
    "pdfjs-dist": "^4.9.155"
  },
  "devDependencies": {
    "puppeteer": "^25.12.0"
  }
}
